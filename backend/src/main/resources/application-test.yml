spring:
  datasource:
    url: jdbc:mysql://localhost:3306/g2u_admin_test?useSSL=false&allowPublicKeyRetrieval=true
    username: g2u
    password: g2u_password
  jpa:
    hibernate:
      ddl-auto: create-drop
  liquibase:
    enabled: false
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test-client-id
            client-secret: test-client-secret
